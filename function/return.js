//by default the return is undefined if you don't declare it
function say(message) {
  ans = message;
  console.log(ans);
}

// console.log("ashutosh"+say("hello"));
// here the function will return undefined so the answer would be ashutoshundefined

function sayhi(message) {
  ans = message;
  console.log(ans);
}
// if we assign the function to a variable then the return value is always undefined unless it's defined